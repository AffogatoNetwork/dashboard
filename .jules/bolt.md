## 2026-07-27 - ApolloClient Initialization\n**Learning:** ApolloClient instances initialized inside React component trees (e.g., inside the App component) get destroyed and re-initialized on every render, destroying the GraphQL cache and causing performance overhead.\n**Action:** Always initialize ApolloClient instances outside of the React component tree at the module level to ensure cache persistence and prevent unnecessary re-initialization.
